// ======= Toast Trigger for Contact Form =======
function showToast() {
  const toastElement = document.getElementById('contactToast');
  const toast = new bootstrap.Toast(toastElement);
  toast.show();
}
